Stock Intelligence has been rendered for namespace {{ .Values.namespace.name }}.

{{- if .Values.gateway.enabled }}
Hosts:
{{- range .Values.gateway.hostnames }}
  - https://{{ . }}
{{- end }}
{{- end }}
Gateway parent: {{ .Values.gateway.parentRef.namespace }}/{{ .Values.gateway.parentRef.name }}

Check the rollout:
  kubectl get pods,svc,pvc,cronjob,httproute -n {{ .Values.namespace.name }}

The chart contains a placeholder database password unless secrets.existingSecret
is set or secrets.postgresPassword is overridden. Do not expose that placeholder.
